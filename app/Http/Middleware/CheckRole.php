<?php


/*
public function handle($request, Closure $next, ...$roles)
{
    if (!auth()->check()) {
        return redirect('/login');
    }

    if (!auth()->user()->hasAnyRole($roles)) {
        abort(403);
    }

    return $next($request);
}
*/